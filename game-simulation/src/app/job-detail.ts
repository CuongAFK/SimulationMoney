export interface JobDetail {
    id: string;         // ID người dùng
    ma: string;        // Mã công việc
    namKinhNghiem: number; // Số năm kinh nghiệm
    ten?: string;  // Tên công việc (nếu cần)
}
